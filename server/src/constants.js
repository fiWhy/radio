const path = require('path');

const videoFolder = path.join(__dirname, '../../assets/video'),
  audioFolder = path.join(__dirname, '../../assets/audio');

module.exports = {
  videoFolder,
  audioFolder
};
